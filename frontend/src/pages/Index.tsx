import type { NextPage } from 'next'

const Home: NextPage = () => (
  <main className="min-h-screen grid place-items-center p-10">
    <h1 className="text-2xl font-semibold">Frontend is installed (Lovable UI)</h1>
    <p className="opacity-70">Replace this with your home view (e.g., src/components/pageviews/Index)</p>
  </main>
);
export default Home;
